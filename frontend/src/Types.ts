export type ItemOracleType = {
    id: number,
    job: string,
    status: string,
    type: string,
    jobClass: string,
    assembly: string,
    assemblyDescription: string,
    quantity: number,
    quantityCompleted: number,
    yield: number,
    quantityScrapped: number,
    quantityRemaining: number,
    startDate: string,
    dateCompleted: string,
    dateClosed: string,
    extraColumn: string
}